import 'package:flutter/material.dart';

class OnboardPageThird extends StatelessWidget {
  const OnboardPageThird({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
